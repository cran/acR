% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_tokenize.R
\name{ac_tokenize}
\alias{ac_tokenize}
\title{Tokenizar textos de um corpus acR}
\usage{
ac_tokenize(
  corpus,
  token = c("word"),
  n = 1L,
  keep_empty = FALSE,
  drop_punct = FALSE,
  ...
)
}
\arguments{
\item{corpus}{Objeto de classe \code{\link[=ac_corpus]{ac_corpus()}}.}

\item{token}{Tipo de tokenizacao desejada. Atualmente apenas
\code{"word"} e suportado (padrao), reservado para futura expansao.}

\item{n}{Tamanho do n-grama. Deve ser um inteiro maior ou igual
a 1. Para \code{n = 1}, o resultado sao tokens individuais; para
\code{n = 2}, bigramas (\code{"A B"}), para \code{n = 3}, trigramas etc.}

\item{keep_empty}{Logico. Se \code{FALSE} (padrao), documentos que
resultarem em texto vazio apos a limpeza nao geram linhas na
saida. Se \code{TRUE}, cada documento vazio gera uma linha com
\code{token = NA} quando \code{n = 1}.}

\item{drop_punct}{Logico. Se \code{TRUE}, remove da sequencia tokens
que consistem apenas de pontuacao (por exemplo \code{"!"}, \code{"..."})
antes de construir n-gramas. Tokens que misturam letras e
pontuacao (por exemplo \code{"ola,"}) sao mantidos.}

\item{...}{Ignorado, reservado para argumentos futuros.}
}
\value{
Um \code{\link[tibble:tibble]{tibble::tibble()}} com colunas:
\itemize{
\item \code{doc_id}: identificador do documento (herdado de \code{ac_corpus});
\item \code{token_id}: posicao (1, 2, 3, ...) do token ou n-grama no documento;
\item \code{token}: texto do token ou n-grama.
}
}
\description{
\code{ac_tokenize()} recebe um objeto \code{\link[=ac_corpus]{ac_corpus()}} e retorna um
\code{tibble} em formato \emph{tidy}, com um token por linha, no estilo
usado em análises de texto no ecossistema tidy.

A função implementa tokenização em palavras (n = 1) ou
n-gramas de tamanho arbitrário (n > 1), usando janelas
contíguas de tokens dentro de cada documento.

É o segundo passo canônico do pipeline quantitativo, geralmente entre
\code{\link[=ac_clean]{ac_clean()}} e \code{\link[=ac_count]{ac_count()}}. Para análises que dependem de expressões
compostas (\"reforma tributária\", \"desenvolvimento sustentável\"),
use \code{n = 2L} para bigramas — cada n-grama vira uma unidade de contagem
independente.
}
\examples{
df <- data.frame(
  id    = c("d1", "d2"),
  texto = c(
    "O deputado do PT falou na CCJ.",
    "Votar pra valer, agora!"
  )
)

corp <- ac_corpus(df, text = texto, docid = id)

# Tokenizacao simples em palavras
tokens <- ac_tokenize(corp)
tokens

# Removendo tokens que sao apenas pontuacao
df2 <- data.frame(
  id    = "d1",
  texto = "Ola, mundo ! ..."
)
corp2   <- ac_corpus(df2, text = texto, docid = id)
tokens2 <- ac_tokenize(corp2, drop_punct = TRUE)
tokens2

# Bigramas
ac_tokenize(corp, n = 2)

}
\seealso{
\code{\link[=ac_corpus]{ac_corpus()}}, \code{\link[=ac_clean]{ac_clean()}}
}
\concept{corpus}
