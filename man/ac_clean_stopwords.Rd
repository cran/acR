% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_clean.R
\name{ac_clean_stopwords}
\alias{ac_clean_stopwords}
\title{Inspecionar e editar stopwords extras do acR}
\usage{
ac_clean_stopwords(
  add = NULL,
  remove = NULL,
  preset = c("empty", "pt", "pt-br-extended", "pt-legislativo")
)
}
\arguments{
\item{add}{Vetor \code{character} com termos a adicionar ao vetor padrão.}

\item{remove}{Vetor \code{character} com termos a remover do vetor padrão.}

\item{preset}{String indicando o ponto de partida:
\code{"empty"} (padrão), \code{"pt"}, \code{"pt-br-extended"} ou \code{"pt-legislativo"}.}
}
\value{
Vetor \code{character} de stopwords pronto para passar a
\code{ac_clean(..., extra_stopwords = ...)}.
}
\description{
\code{ac_clean_stopwords()} retorna (e opcionalmente modifica) o vetor de
stopwords adicionais que o pesquisador pode passar a \code{\link[=ac_clean]{ac_clean()}} via
\code{extra_stopwords}. Funciona como um ponto de partida editável: o
pesquisador inspeciona o vetor, adiciona ou remove termos conforme o
corpus, e passa o resultado para \code{extra_stopwords}.
}
\examples{
# Ver o vetor padrão vazio e adicionar termos
sw <- ac_clean_stopwords(add = c("nobre", "ilustre", "respeitavel"))
print(sw)

# Partir do preset legislativo e remover termos que interessam ao corpus
sw <- ac_clean_stopwords(
  preset = "pt-legislativo",
  remove = c("lei", "projeto")   # manter: são relevantes para a análise
)

# Usar na limpeza
# ac_clean(corpus, remove_stopwords = "pt", extra_stopwords = sw)

}
\seealso{
\code{\link[=ac_clean]{ac_clean()}}
}
