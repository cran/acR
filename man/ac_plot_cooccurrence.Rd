% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_plot_cooccurrence.R
\name{ac_plot_cooccurrence}
\alias{ac_plot_cooccurrence}
\title{Visualizar rede de co-ocorrência de termos}
\usage{
ac_plot_cooccurrence(
  cooc,
  top_n = 50L,
  weight = c("cooc", "pmi", "dice"),
  layout = "fr",
  node_color = "#0072B2",
  edge_color = "grey70",
  title = NULL,
  ...
)
}
\arguments{
\item{cooc}{Tibble com co-ocorrências, saída de \code{\link[=ac_cooccurrence]{ac_cooccurrence()}}.}

\item{top_n}{Número de pares mais frequentes a exibir. Padrão: \code{50}.}

\item{weight}{Coluna a usar como peso das arestas:
\code{"cooc"} (padrão), \code{"pmi"} ou \code{"dice"}.}

\item{layout}{Layout do grafo. Qualquer layout suportado por
\code{ggraph::ggraph()}: \code{"fr"} (Fruchterman-Reingold, padrão),
\code{"kk"}, \code{"circle"}, etc.}

\item{node_color}{Cor dos nós. Padrão: \code{"#0072B2"}.}

\item{edge_color}{Cor das arestas. Padrão: \code{"grey70"}.}

\item{title}{Título do gráfico. Padrão: \code{NULL}.}

\item{...}{Ignorado.}
}
\value{
Objeto \code{ggplot}.
}
\description{
\code{ac_plot_cooccurrence()} gera um gráfico de rede a partir de um tibble de
co-ocorrências (saída de \code{\link[=ac_cooccurrence]{ac_cooccurrence()}}), usando \code{ggplot2} e \code{ggraph}.
}
\examples{
# 1. Corpus pequeno de tres documentos
df <- data.frame(
  id = c("d1", "d2", "d3"),
  texto = c(
    "democracia participacao cidadania",
    "participacao politica democracia",
    "cidadania direitos participacao"
  )
)

# 2. Calcular co-ocorrencias de termos
corpus <- ac_corpus(df, text = texto, docid = id) |> ac_clean()
cooc <- ac_cooccurrence(ac_tokenize(corpus), min_count = 1)

# 3. Rede visualizada com ggraph (dependencia opcional)
if (requireNamespace("ggraph", quietly = TRUE)) {
  ac_plot_cooccurrence(cooc)
}

}
\seealso{
\code{\link[=ac_cooccurrence]{ac_cooccurrence()}}
}
\concept{visualization}
