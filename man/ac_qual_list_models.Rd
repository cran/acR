% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_qual_models.R
\name{ac_qual_list_models}
\alias{ac_qual_list_models}
\title{Listar modelos LLM disponíveis para análise de conteúdo}
\usage{
ac_qual_list_models(
  provider = "all",
  filter = NULL,
  sort_by = c("cost", "name", "context"),
  live = FALSE,
  ...
)
}
\arguments{
\item{provider}{Provedor(es) a listar. Pode ser \code{"all"} (padrão) ou um
ou mais de: \code{"anthropic"}, \code{"openai"}, \code{"google"}, \code{"groq"},
\code{"deepseek"}, \code{"mistral"}, \code{"ollama"}.}

\item{filter}{String para filtrar modelos por nome ou ID
(ex: \code{"claude"}, \code{"gpt-4"}). Padrão: \code{NULL} (sem filtro).}

\item{sort_by}{Como ordenar os resultados: \code{"cost"} (padrão, menor custo
primeiro), \code{"name"}, \code{"context"} (maior janela de contexto primeiro).}

\item{live}{Lógico. Se \code{TRUE}, consulta a API do provedor ao vivo via
\verb{ellmer::models_*()}. Requer chave de API. Padrão: \code{FALSE}.}

\item{...}{Ignorado.}
}
\value{
Tibble com colunas:
\itemize{
\item \code{provider}: nome do provedor;
\item \code{model_id}: identificador do modelo para uso em \code{\link[=ac_qual_code]{ac_qual_code()}};
\item \code{name}: nome legível;
\item \code{context_k}: janela de contexto em milhares de tokens;
\item \code{cost_input}: custo por 1M tokens de entrada (USD), \code{NA} se gratuito/local;
\item \code{cost_output}: custo por 1M tokens de saída (USD);
\item \code{tier}: categoria (\code{"frontier"}, \code{"balanced"}, \code{"fast"}, \code{"free"}, \code{"local"});
\item \code{pt_support}: suporte estimado ao português (\code{"alto"}, \code{"medio"}, \code{"baixo"});
\item \code{acr_string}: string pronta para uso em \code{model = ...} no \code{acR}.
}
}
\description{
\code{ac_qual_list_models()} retorna um tibble com os modelos LLM disponíveis
para uso com \code{\link[=ac_qual_code]{ac_qual_code()}}, incluindo informações de custo, janela de
contexto e compatibilidade com análise de conteúdo qualitativa em
Ciências Sociais.

Dois modos de operação:
\itemize{
\item \strong{\code{live = FALSE}} (padrão): usa banco interno curado, funciona offline.
\item \strong{\code{live = TRUE}}: consulta a API do provedor via \verb{ellmer::models_*()}
para obter a lista mais atualizada. Requer chave de API configurada.
}
}
\examples{
# Listar todos os modelos do banco interno
ac_qual_list_models()

# Só modelos Anthropic
ac_qual_list_models(provider = "anthropic")

# Modelos baratos com suporte a PT
ac_qual_list_models(sort_by = "cost") |>
  dplyr::filter(pt_support == "alto", cost_input < 1)

}
\seealso{
\code{\link[=ac_qual_recommend_model]{ac_qual_recommend_model()}}, \code{\link[=ac_qual_code]{ac_qual_code()}}
}
\concept{qualitative}
