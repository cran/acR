% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_corpus.R
\name{ac_corpus}
\alias{ac_corpus}
\title{Construir um corpus para análise de conteúdo}
\usage{
ac_corpus(data, text = NULL, docid = NULL, meta = NULL, lang = "pt", ...)
}
\arguments{
\item{data}{Entrada do corpus. Pode ser:
\itemize{
\item um \code{data.frame} ou \code{tibble} com pelo menos uma coluna de texto;
\item um vetor \code{character} com textos (um documento por elemento);
\item um objeto \code{corpus} do pacote \code{quanteda}.
}}

\item{text}{Nome da coluna de texto, sem aspas (usa \emph{tidyselect} /
\emph{non-standard evaluation}). Apenas relevante quando \code{data} é um
\code{data.frame}. Se \code{NULL} (padrão), a função tenta detectar automaticamente
uma coluna chamada \code{text}, \code{texto}, \code{doc}, \code{content} ou \code{conteudo}.}

\item{docid}{Nome da coluna de identificador único, sem aspas. Se \code{NULL}
(padrão), gera IDs sequenciais no formato \code{doc_1}, \code{doc_2}, ...}

\item{meta}{Colunas de metadados a preservar, via \emph{tidyselect} (ex:
\code{c(partido, data, regiao)} ou \code{tidyselect::starts_with("var_")}).
Se \code{NULL}, preserva todas as demais colunas do \code{data.frame}.}

\item{lang}{Código ISO do idioma do corpus (padrão: \code{"pt"}). Utilizado
em etapas posteriores (lematização, stopwords, léxicos).}

\item{...}{Argumentos reservados para extensão futura. No momento,
são ignorados com aviso se nomeados de forma desconhecida.}
}
\value{
Um objeto de classe \code{ac_corpus} (que herda de \code{tbl_df},
\code{tbl}, \code{data.frame}) com as colunas:
\itemize{
\item \code{doc_id} (character): identificador único do documento.
\item \code{text} (character): texto do documento.
\item Demais colunas de metadados, preservadas de \code{data}.
O objeto traz também o atributo \code{lang} indicando o idioma.
}
}
\description{
\code{ac_corpus()} é a porta de entrada do pipeline do pacote \code{acR}. Constrói
um objeto estruturado de corpus a partir de um \code{data.frame}, um vetor
de caracteres, ou um corpus do \code{quanteda}, validando a entrada e
preservando metadados associados aos documentos.
}
\details{
A função realiza cinco validações obrigatórias e falha cedo em caso
de problema:
\enumerate{
\item \code{data} deve ser de um dos tipos suportados;
\item a coluna de texto deve existir (ou ser detectável automaticamente);
\item a coluna de texto não pode ser inteiramente \code{NA} ou vazia;
\item \code{doc_id} não pode ter valores duplicados;
\item \code{doc_id} não pode conter \code{NA}.
}

Documentos com texto vazio ou \code{NA} geram aviso (\code{warning}) mas são
mantidos no corpus com texto \code{""} — o usuário decide se filtra depois.
}
\examples{
# A partir de um data.frame
df <- data.frame(
  id = c("a", "b", "c"),
  texto = c("Primeiro texto.", "Segundo texto.", "Terceiro."),
  partido = c("PT", "PL", "MDB")
)
corpus <- ac_corpus(df, text = texto, docid = id, meta = partido)
corpus

# A partir de um vetor character (doc_id gerado automaticamente)
ac_corpus(c("Texto um.", "Texto dois."))

# Detecção automática da coluna de texto
df2 <- data.frame(text = c("A", "B"), autor = c("X", "Y"))
ac_corpus(df2)

}
\concept{corpus}
