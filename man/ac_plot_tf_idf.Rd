% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_plot_tf_idf.R
\name{ac_plot_tf_idf}
\alias{ac_plot_tf_idf}
\title{Plotar termos mais caracteristicos por tf-idf}
\usage{
ac_plot_tf_idf(
  x,
  by = NULL,
  n = NULL,
  style = c("default", "ipea"),
  flip = TRUE
)
}
\arguments{
\item{x}{Um \code{data.frame} ou \code{\link[tibble:tibble]{tibble::tibble()}} contendo, no minimo,
as colunas \code{token} e \code{tf_idf}.}

\item{by}{Vetor de nomes de colunas em \code{x} a serem usados como grupos
de facetas. Se \code{NULL} (padrao), produz um unico grafico. Se nao for
\code{NULL}, cria facetas por combinacao das colunas informadas.}

\item{n}{Numero de termos a exibir. Se \code{NULL} (padrao), usa todas as
linhas de \code{x}. Se informado, seleciona os top \code{n} termos por \code{tf_idf}
no geral ou em cada grupo definido por \code{by}.}

\item{style}{Estilo grafico. Pode ser \code{"default"} (padrao) ou \code{"ipea"}.
Quando \code{"ipea"}, a funcao tenta aplicar \code{ipeaplot::theme_ipea()}.}

\item{flip}{Logico. Se \code{TRUE} (padrao), usa barras horizontais com
\code{\link[ggplot2:coord_flip]{ggplot2::coord_flip()}}.}
}
\value{
Um objeto \code{ggplot}.
}
\description{
\code{ac_plot_tf_idf()} cria um grafico de barras com os termos mais
caracteristicos a partir de uma tabela com a coluna \code{tf_idf}, tipicamente
gerada por \code{\link[=ac_tf_idf]{ac_tf_idf()}}.

A funcao usa \link[ggplot2:ggplot2-package]{ggplot2} como base e pode, opcionalmente, aplicar o
estilo editorial do pacote \code{ipeaplot}.
}
\examples{
# 1. Corpus de 3 discursos com metadado de partido
df <- data.frame(
  id      = c("d1", "d2", "d3"),
  texto   = c(
    "O deputado do PT falou na CCJ.",
    "O deputado do PL falou novamente.",
    "O senador do PT falou na CCJ."
  ),
  partido = c("PT", "PL", "PT"),
  stringsAsFactors = FALSE
)

# 2. Pipeline padrao: corpus -> frequencia -> tf-idf
corp  <- ac_corpus(df, text = texto, docid = id, meta = partido)
freq  <- ac_count(corp)
tfidf <- ac_tf_idf(freq)

# 3. Grafico dos 10 termos com maior tf-idf (mais distintivos)
ac_plot_tf_idf(tfidf, n = 10)

freq_by <- ac_count(corp, by = "partido")
tfidf_by <- ac_tf_idf(freq_by, by = "partido")
ac_plot_tf_idf(tfidf_by, by = "partido", n = 5)

}
\seealso{
\code{\link[=ac_count]{ac_count()}}, \code{\link[=ac_tf_idf]{ac_tf_idf()}}, \code{\link[=ac_top_terms]{ac_top_terms()}}
}
\concept{corpus}
