% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_qual_report.R
\name{ac_qual_report_full}
\alias{ac_qual_report_full}
\title{Gerar relatório consolidado de múltiplas variáveis de conteúdo}
\usage{
ac_qual_report_full(
  variables,
  chat = NULL,
  title = NULL,
  author = NULL,
  method = NULL,
  format = c("md", "html"),
  path = NULL,
  lang = c("pt", "en")
)
}
\arguments{
\item{variables}{Lista \strong{nomeada} onde cada elemento é uma lista com:
\itemize{
\item \code{coded}: tibble com resultado de \code{\link[=ac_qual_code]{ac_qual_code()}} (obrigatório).
\item \code{codebook}: objeto \code{ac_codebook} (obrigatório).
\item \code{reliability}: opcional, saída de \code{\link[=ac_qual_reliability]{ac_qual_reliability()}} ou
\code{\link[=ac_qual_irr]{ac_qual_irr()}}.
Os nomes da lista viram os títulos das seções do relatório.
}}

\item{chat}{Opcional. Objeto \code{Chat} do \code{ellmer} ou string de modelo
\code{"provider/model"}. Aplica-se a todas as variáveis (assumindo que
todas foram classificadas com o mesmo modelo — se não, deixe \code{NULL}).}

\item{title}{Título do relatório consolidado. Padrão: \code{"Relatório consolidado de análise de conteúdo"}.}

\item{author}{Autor(es) do estudo.}

\item{method}{Descrição do método de coleta do corpus.}

\item{format}{\code{"md"} (padrão) ou \code{"html"}.}

\item{path}{Caminho do arquivo destino. Se \code{NULL}, usa \code{tempfile()}.}

\item{lang}{\code{"pt"} (padrão) ou \code{"en"}.}
}
\value{
Invisível: caminho do arquivo gerado.
}
\description{
\code{ac_qual_report_full()} gera \strong{um único} documento (Markdown ou HTML)
consolidando resultados de N variáveis de codificação qualitativa —
cada uma com seu próprio codebook, rodada de \code{\link[=ac_qual_code]{ac_qual_code()}} e,
opcionalmente, métricas de \code{\link[=ac_qual_reliability]{ac_qual_reliability()}} ou \code{\link[=ac_qual_irr]{ac_qual_irr()}}.

É a resposta natural ao caso de uso mais comum de análise de conteúdo
categorial (Krippendorff, 2018): um estudo tem várias variáveis de
conteúdo (tom, posicionamento, tema, framing…). Antes desta função, o
pesquisador precisava chamar \code{\link[=ac_qual_report]{ac_qual_report()}} N vezes e emendar os
relatórios manualmente, ou escrever um consolidador ad hoc por projeto.

Aceita variáveis \code{multilabel = TRUE} e \code{multilabel = FALSE} no mesmo
relatório — cada seção herda a apresentação apropriada para o tipo.
}
\examples{
# Duas variaveis de conteudo, cada uma com seu codebook
cb_tom <- ac_qual_codebook(
  name = "tom",
  instructions = "Classifique o tom.",
  categories = list(
    pos = list(definition = "Positivo.", label = "Positivo"),
    neg = list(definition = "Negativo.", label = "Negativo")
  )
)
cb_pos <- ac_qual_codebook(
  name = "posicao",
  instructions = "Classifique a posicao.",
  categories = list(
    favor  = list(definition = "A favor.",  label = "A favor"),
    contra = list(definition = "Contra.",   label = "Contra")
  )
)

coded_tom <- tibble::tibble(
  doc_id = paste0("d", 1:3),
  categoria = c("pos", "neg", "pos"),
  confidence_score = c(1, 0.67, 1)
)
coded_pos <- tibble::tibble(
  doc_id = paste0("d", 1:3),
  categoria = c("favor", "contra", "favor"),
  confidence_score = c(1, 1, 0.67)
)

arquivo <- tempfile(fileext = ".md")
ac_qual_report_full(
  variables = list(
    "Tom do discurso" = list(coded = coded_tom, codebook = cb_tom),
    "Posicao politica" = list(coded = coded_pos, codebook = cb_pos)
  ),
  path = arquivo,
  author = "Fulano de Tal"
)

}
\seealso{
\code{\link[=ac_qual_report]{ac_qual_report()}} para relatório de uma única variável.
}
\concept{qualitative}
