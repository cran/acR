% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_theme.R
\name{theme_ac}
\alias{theme_ac}
\title{Tema visual consistente do acR}
\usage{
theme_ac(base_size = 12, base_family = "")
}
\arguments{
\item{base_size}{Tamanho base da fonte. Padrão: \code{12}.}

\item{base_family}{Família tipográfica. Padrão: \code{""} (usa sistema).}
}
\value{
Um objeto \code{ggplot2::theme}.
}
\description{
\code{theme_ac()} retorna um tema \code{ggplot2} minimalista e consistente, usado
por todos os \verb{ac_plot_*()} do pacote. Deriva de \code{ggplot2::theme_minimal()}
com ajustes editoriais: tipografia mais compacta, gridlines suaves,
títulos em negrito com espaçamento negativo (visual editorial).

Também expõe \code{\link[=ac_palette]{ac_palette()}} para uma paleta categórica coerente com o
tema (compatível com acessibilidade AA).
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  ggplot2::ggplot(mtcars, ggplot2::aes(mpg, wt)) +
    ggplot2::geom_point(color = ac_palette()[1]) +
    ggplot2::labs(title = "MPG vs. peso", subtitle = "Tema editorial acR") +
    theme_ac()
}

}
\seealso{
\code{\link[=ac_palette]{ac_palette()}}
}
\concept{visualization}
