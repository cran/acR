% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_qual_codebook.R
\name{ac_qual_codebook_merge}
\alias{ac_qual_codebook_merge}
\title{Fundir dois codebooks em um}
\usage{
ac_qual_codebook_merge(
  cb1,
  cb2,
  name = NULL,
  on_conflict = c("error", "keep_first", "keep_second", "rename_second"),
  instructions = NULL
)
}
\arguments{
\item{cb1}{Objeto \code{ac_codebook} (base).}

\item{cb2}{Objeto \code{ac_codebook} (a fundir).}

\item{name}{Nome do codebook resultante. Padrão: \code{"cb1_cb2"}.}

\item{on_conflict}{Estratégia em caso de categorias com o mesmo nome:
\code{"error"} (padrão), \code{"keep_first"}, \code{"keep_second"} ou \code{"rename_second"}.}

\item{instructions}{Instrução geral do novo codebook. Se \code{NULL}, usa a de \code{cb1}.}
}
\value{
Objeto \code{ac_codebook} fundido.
}
\description{
\code{ac_qual_codebook_merge()} combina as categorias de dois objetos \code{ac_codebook}
em um único codebook, com controle de conflitos de nomes.
}
\examples{
# Dois codebooks pequenos que cobrem dimensoes distintas
cb_tom <- ac_qual_codebook(
  name         = "tom",
  instructions = "Classifique o tom.",
  categories   = list(
    positivo = list(definition = "Tom positivo."),
    negativo = list(definition = "Tom negativo.")
  )
)
cb_estilo <- ac_qual_codebook(
  name         = "estilo",
  instructions = "Classifique o estilo retorico.",
  categories   = list(
    pathos = list(definition = "Apelo emocional."),
    logos  = list(definition = "Apelo racional.")
  )
)

# Fundir em um unico codebook com 4 categorias
cb <- ac_qual_codebook_merge(cb_tom, cb_estilo, name = "tom_estilo")
names(cb$categories)  # "positivo" "negativo" "pathos" "logos"

}
\concept{qualitative}
