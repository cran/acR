% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_import.R
\name{ac_import}
\alias{ac_import}
\title{Importar arquivos para um corpus acR}
\usage{
ac_import(path, text_field = NULL, lang_ocr = "por", id_from = "filename", ...)
}
\arguments{
\item{path}{Caminho para um arquivo, vetor de arquivos ou glob
(ex: \code{'dados/*.docx'}). Formatos suportados: \code{.pdf}, \code{.doc}, \code{.docx},
\code{.xlsx}, \code{.xls}, \code{.csv}, \code{.txt}, \code{.json}, \code{.png}, \code{.jpg}, \code{.jpeg},
\code{.tiff}.}

\item{text_field}{Nome da coluna que contem o texto, para arquivos
tabulares (\code{.xlsx}, \code{.xls}, \code{.csv}). Obrigatorio nesses formatos.}

\item{lang_ocr}{Lingua para OCR em imagens e PDFs escaneados.
Padrao: \code{'por'} (portugues). Use \code{tesseract::tesseract_info()} para
ver linguas instaladas.}

\item{id_from}{Como gerar os IDs dos documentos. \code{'filename'} (padrao)
usa o nome do arquivo sem extensao. \code{'rownum'} usa numeros sequenciais.
Ou um vetor de strings com os IDs desejados.}

\item{...}{Argumentos adicionais passados para \code{readtext::readtext()}.}
}
\value{
Um objeto \code{ac_corpus} pronto para uso com todas as funcoes do acR.
}
\description{
Importa arquivos de texto em diferentes formatos (PDF, Word, Excel, CSV,
TXT, JSON, imagens com OCR) e retorna diretamente um objeto \code{ac_corpus}.
Suporta caminhos individuais, vetores de arquivos e globs (\verb{dados/*.pdf}).

É a \strong{via de entrada principal} do pipeline quando você trabalha com
arquivos em disco (relatórios, transcrições, PDFs escaneados). Faz o
que \code{ac_corpus()} faria a partir de um \code{data.frame}, mas cobrindo o
passo anterior: extração de texto de formatos heterogêneos com detecção
automática pelo sufixo do arquivo. Para PDFs escaneados e imagens,
aciona OCR via \code{tesseract} com português como padrão.
}
\details{
O \code{ac_import()} detecta automaticamente o formato pelo extension e
aciona o parser correto:
\itemize{
\item \strong{PDF com texto selecionavel}: \code{readtext} + \code{pdftools}
\item \strong{PDF escaneado / imagem}: \code{tesseract} (OCR)
\item \strong{Word} (\code{.doc}, \code{.docx}): \code{readtext}
\item \strong{Excel} (\code{.xlsx}, \code{.xls}): \code{readtext} (requer \code{text_field})
\item \strong{CSV}: \code{readtext} (requer \code{text_field})
\item \strong{TXT / JSON}: \code{readtext}
\item \strong{Pasta inteira / glob}: todos os arquivos compativeis de uma vez
}

A ordem dos documentos no corpus resultante segue a ordem de entrada em
\code{path} (ou a ordem alfabetica retornada por \code{Sys.glob()}), independente
de o parser ser OCR ou readtext. IDs duplicados (dois arquivos com o
mesmo \code{basename}) sao desambiguados automaticamente com um sufixo \verb{_2},
\verb{_3}, ... e um aviso e emitido.

\strong{Dependencias opcionais}: \code{readtext} e \code{tesseract} nao sao importados
automaticamente -- o \code{ac_import()} verifica se estao instalados e
orienta a instalacao caso necessario.
}
\examples{
\dontrun{
# PDF com texto selecionavel
corpus <- ac_import('relatorio.pdf')

# Pasta inteira de Word
corpus <- ac_import('proposicoes/*.docx')

# Excel -- indicar a coluna de texto
corpus <- ac_import('respostas.xlsx', text_field = 'resposta')

# PDF escaneado (OCR em portugues)
corpus <- ac_import('ata_manuscrita.pdf', lang_ocr = 'por')

# Imagem (OCR)
corpus <- ac_import('captura.png')

# Pasta mista (PDF + DOCX + TXT)
corpus <- ac_import('dados/*')
}

}
\references{
Benoit, K., et al. (2018). readtext: Import and Handling for Plain and
Formatted Text Files. R package.
\url{https://CRAN.R-project.org/package=readtext}

Ooms, J. (2024). tesseract: Open Source OCR Engine. R package.
\url{https://CRAN.R-project.org/package=tesseract}
}
\seealso{
\code{\link[=ac_corpus]{ac_corpus()}}, \code{\link[=ac_clean]{ac_clean()}}, \code{\link[=ac_tokenize]{ac_tokenize()}}
}
