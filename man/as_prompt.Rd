% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_qual_codebook.R
\name{as_prompt}
\alias{as_prompt}
\alias{as_prompt.default}
\alias{as_prompt.ac_codebook}
\title{Converter codebook em system prompt para LLM}
\usage{
as_prompt(x, ...)

\method{as_prompt}{default}(x, ...)

\method{as_prompt}{ac_codebook}(
  x,
  reasoning = TRUE,
  reasoning_length = c("short", "medium", "detailed"),
  ...
)
}
\arguments{
\item{x}{Objeto a converter (para \code{as_prompt.ac_codebook}: um \code{ac_codebook}).}

\item{...}{Argumentos adicionais passados ao método.}

\item{reasoning}{Lógico. Se \code{TRUE}, inclui campo de raciocínio no JSON de saída. Padrão: \code{TRUE}.}

\item{reasoning_length}{Extensão do raciocínio: \code{"short"}, \code{"medium"} ou \code{"detailed"}.}
}
\value{
String com o system prompt (invisível).
}
\description{
\code{as_prompt()} é um genérico S3 que converte um objeto em system prompt
formatado para uso com LLMs. O método \code{as_prompt.ac_codebook()} gera
o prompt a partir de um \code{ac_codebook}, incluindo instruções, categorias,
exemplos, pesos e, opcionalmente, raciocínio estruturado.
}
\examples{
# Codebook base
cb <- ac_qual_codebook(
  name         = "tom",
  instructions = "Classifique o tom do texto.",
  categories   = list(
    formal   = list(definition = "Linguagem tecnica e impessoal."),
    informal = list(definition = "Linguagem coloquial ou emotiva.")
  )
)

# Gerar o system prompt para uso direto com objetos Chat do ellmer
prompt <- as_prompt(cb, reasoning = TRUE, reasoning_length = "short")
substr(prompt, 1, 200)  # inspecionar o comeco do prompt

}
\concept{qualitative}
