% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_count.R
\name{ac_count}
\alias{ac_count}
\title{Contar frequências de tokens ou n-gramas em um corpus}
\usage{
ac_count(corpus, n = 1L, drop_punct = FALSE, by = NULL, sort = TRUE, ...)
}
\arguments{
\item{corpus}{Objeto de classe \code{\link[=ac_corpus]{ac_corpus()}}.}

\item{n}{Tamanho do n-grama a ser tokenizado. Encaminhado para
\code{\link[=ac_tokenize]{ac_tokenize()}}. Veja \code{?ac_tokenize} para detalhes.}

\item{drop_punct}{Logico. Se \code{TRUE}, remove tokens compostos apenas
por pontuacao antes de calcular as frequencias (via
\code{drop_punct = TRUE} em \code{\link[=ac_tokenize]{ac_tokenize()}}).}

\item{by}{Vetor de nomes de colunas de metadados em \code{corpus} a serem
usados como grupos de agregacao. Se \code{NULL} (padrao), as contagens
sao feitas por documento (\code{doc_id}). Se nao for \code{NULL}, as colunas
indicadas sao usadas como grupos e \code{doc_id} nao entra no resultado.}

\item{sort}{Logico. Se \code{TRUE} (padrao), ordena a saida em ordem
decrescente de frequencia (\code{n}).}

\item{...}{Argumentos adicionais encaminhados para
\code{\link[=ac_tokenize]{ac_tokenize()}} (por exemplo, \code{keep_empty}).}
}
\value{
Um \code{\link[tibble:tibble]{tibble::tibble()}}:
\itemize{
\item Se \code{by = NULL}: colunas \code{doc_id}, \code{token}, \code{n};
\item Se \code{by} nao nulo: colunas \code{by}, \code{token}, \code{n}.
}
}
\description{
\code{ac_count()} calcula frequências de tokens ou n-gramas a partir de
um objeto \code{\link[=ac_corpus]{ac_corpus()}}, usando internamente \code{\link[=ac_tokenize]{ac_tokenize()}}
seguido de uma agregação com \code{\link[dplyr:count]{dplyr::count()}}.

É o insumo direto de quase toda análise quantitativa do pacote:
\code{\link[=ac_top_terms]{ac_top_terms()}} pega o topo, \code{\link[=ac_tf_idf]{ac_tf_idf()}} recalibra por
distintividade, \code{\link[=ac_keyness]{ac_keyness()}} compara grupos, \code{\link[=ac_wordcloud]{ac_wordcloud()}}
visualiza. O argumento \code{by} decide se a unidade de análise é o
documento ou o grupo agregado.

Pode operar em dois níveis:
\itemize{
\item por documento (\code{by = NULL}): contagens por \code{doc_id} e \code{token};
\item por metadados (\code{by = c("partido", ...)}): contagens por
variáveis de agrupamento e \code{token} (agregando vários documentos).
}
}
\examples{
df <- data.frame(
  id      = c("d1", "d2", "d3"),
  texto   = c(
    "O deputado do PT falou na CCJ.",
    "O deputado do PL falou novamente.",
    "O senador do PT falou na CCJ."
  ),
  partido = c("PT", "PL", "PT"),
  stringsAsFactors = FALSE
)

corp <- ac_corpus(df, text = texto, docid = id, meta = partido)

# Frequencia de palavras (unigramas) por documento
ac_count(corp)

# Frequencia de palavras por partido
ac_count(corp, by = "partido")

# Frequencia de bigramas por partido, removendo apenas pontuacao
ac_count(corp, n = 2, drop_punct = TRUE, by = "partido")

}
\seealso{
\code{\link[=ac_corpus]{ac_corpus()}}, \code{\link[=ac_clean]{ac_clean()}}, \code{\link[=ac_tokenize]{ac_tokenize()}}
}
\concept{corpus}
