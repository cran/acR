% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_qual_codebook.R
\name{ac_qual_codebook_hybrid}
\alias{ac_qual_codebook_hybrid}
\title{Enriquecer codebook com literatura via LLM (modo híbrido)}
\usage{
ac_qual_codebook_hybrid(
  codebook,
  chat = NULL,
  model = "anthropic/claude-sonnet-4-5",
  concepts = NULL,
  journals = "default",
  n_refs = 3L,
  lang = "pt"
)
}
\arguments{
\item{codebook}{Objeto \code{ac_codebook}.}

\item{chat}{Objeto \code{Chat} do pacote \code{ellmer}. Tem prioridade sobre \code{model}.}

\item{model}{Modelo LLM. Padrão: \code{"anthropic/claude-sonnet-4-5"}.}

\item{concepts}{Lista nomeada com conceitos por categoria (opcional).}

\item{journals}{Periódicos para busca. Padrão: \code{"default"}.}

\item{n_refs}{Número de referências por categoria. Padrão: \code{3L}.}

\item{lang}{Idioma: \code{"pt"} (padrão) ou \code{"en"}.}
}
\value{
Objeto \code{ac_codebook} com definições atualizadas e literatura anexada.
}
\description{
\code{ac_qual_codebook_hybrid()} re-ancora as definições de um \code{ac_codebook}
existente em referências bibliográficas buscadas via LLM, combinando
definições manuais com fundamento teórico induzido da literatura.
}
\examples{
\dontrun{
# Requer conexao com a internet + credenciais da LLM (ANTHROPIC_API_KEY).

# 1. Codebook manual como ponto de partida
cb <- ac_qual_codebook(
  name         = "populismo",
  instructions = "Identifique tom populista no discurso.",
  categories   = list(
    populista     = list(definition = "Apela ao povo contra a elite."),
    nao_populista = list(definition = "Discurso tecnico ou institucional.")
  )
)

# 2. Enriquecer com literatura: busca 3 refs por categoria e reescreve
#    as definicoes com base na literatura recuperada
cb_ancorado <- ac_qual_codebook_hybrid(
  codebook = cb,
  n_refs   = 3L,
  lang     = "pt"
)

# 3. Inspecionar as referencias anexadas a cada categoria
cb_ancorado$categories$populista$references
}

}
\concept{qualitative}
