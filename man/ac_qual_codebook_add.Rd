% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_qual_codebook.R
\name{ac_qual_codebook_add}
\alias{ac_qual_codebook_add}
\title{Adicionar categoria a um codebook existente}
\usage{
ac_qual_codebook_add(codebook, ...)
}
\arguments{
\item{codebook}{Objeto \code{ac_codebook}.}

\item{...}{Categorias a adicionar, nomeadas. Cada elemento deve ser uma
lista com \code{definition} e, opcionalmente, \code{examples_pos}, \code{examples_neg},
\code{weight} e \code{references}.}
}
\value{
Objeto \code{ac_codebook} atualizado.
}
\description{
\code{ac_qual_codebook_add()} adiciona uma ou mais categorias a um \code{ac_codebook}
já criado, sem precisar recriar o objeto do zero. Útil para refinamento
iterativo do codebook durante a análise.
}
\examples{
# Codebook inicial com 2 categorias
cb <- ac_qual_codebook(
  name         = "tom",
  instructions = "Classifique o tom.",
  categories   = list(
    positivo = list(definition = "Tom propositivo."),
    negativo = list(definition = "Tom critico.")
  )
)

# Adicionar uma terceira categoria (nome do argumento vira nome da categoria)
cb <- ac_qual_codebook_add(cb,
  neutro = list(
    definition   = "Tom neutro, sem posicionamento claro.",
    examples_pos = c("O projeto foi apresentado na sessao de hoje.")
  )
)
names(cb$categories)  # "positivo" "negativo" "neutro"

}
\seealso{
\code{\link[=ac_qual_codebook]{ac_qual_codebook()}}, \code{\link[=ac_qual_codebook_remove]{ac_qual_codebook_remove()}}
}
\concept{qualitative}
