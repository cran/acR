% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_plot_wordcloud_comparative.R
\name{ac_plot_wordcloud_comparative}
\alias{ac_plot_wordcloud_comparative}
\title{Nuvem de palavras comparativa entre grupos}
\usage{
ac_plot_wordcloud_comparative(
  corpus,
  group,
  max_words = 50L,
  colors = NULL,
  title = NULL,
  seed = 42L,
  backend = c("auto", "ggwordcloud", "ggplot"),
  ...
)
}
\arguments{
\item{corpus}{Objeto \code{ac_corpus} com coluna de metadado de grupo.}

\item{group}{Coluna de agrupamento (nome sem aspas ou string). Deve
ter pelo menos 2 valores únicos.}

\item{max_words}{Número máximo de palavras por grupo. Padrão: \code{50}.}

\item{colors}{Vetor de cores (uma por grupo, na ordem alfabética dos
grupos). Padrão: as primeiras N cores de \code{\link[=ac_palette]{ac_palette()}} (Okabe-Ito).}

\item{title}{Título do gráfico. Padrão: \code{NULL}.}

\item{seed}{Semente para o posicionamento aleatorio dos termos. Padrao
\code{42L} (garante layout reproduzivel entre chamadas).}

\item{backend}{Motor de renderizacao: \code{"auto"} (padrao, prefere
\code{ggwordcloud} com facets), \code{"ggwordcloud"} ou \code{"ggplot"} (facets
com \code{geom_text} + jitter reproduzivel).}

\item{...}{Ignorado.}
}
\value{
Objeto \code{ggplot}.
}
\description{
\code{ac_plot_wordcloud_comparative()} gera nuvens de palavras comparativas
entre N grupos de documentos, dispostas em facets lado a lado. Usa
TF-IDF (calculado tratando cada grupo como um "documento") para
identificar os termos mais distintivos de cada grupo.

Aceita 2, 3, 4+ grupos: cada grupo vira uma faceta. Para dois grupos
a leitura fica naturalmente lado a lado; para mais, o layout se
organiza em uma linha (ou grade, se muitos grupos).
}
\examples{
# Corpus dividido em dois grupos com vocabulario contrastante
df <- data.frame(
  id     = paste0("d", 1:6),
  texto  = c(
    "democracia participacao popular voto",
    "direitos cidadania liberdade democracia",
    "participacao popular igualdade direitos",
    "mercado economia privatizacao eficiencia",
    "privatizacao mercado livre eficiencia",
    "economia crescimento mercado investimento"
  ),
  grupo = c("A","A","A","B","B","B")
)
corpus <- ac_corpus(df, text = texto, docid = id)

# Nuvem comparativa: termos distintivos de cada grupo
ac_plot_wordcloud_comparative(corpus, group = grupo)

}
\seealso{
\code{\link[=ac_tf_idf]{ac_tf_idf()}}
}
\concept{visualization}
