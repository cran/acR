% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ac_cooccurrence.R
\name{ac_cooccurrence}
\alias{ac_cooccurrence}
\title{Calcular co-ocorrências de termos}
\usage{
ac_cooccurrence(
  corpus,
  window = 5L,
  unit = c("window", "document"),
  measure = c("count", "pmi", "dice"),
  min_count = 2L,
  ...
)
}
\arguments{
\item{corpus}{Objeto \code{ac_corpus} ou tibble com colunas \code{doc_id} e \code{token}
(saída de \code{\link[=ac_tokenize]{ac_tokenize()}}).}

\item{window}{Tamanho da janela deslizante (número de tokens de cada lado).
Padrão: \code{5}. Ignorado se \code{unit = "document"}.}

\item{unit}{Unidade de co-ocorrência: \code{"window"} (padrão) ou \code{"document"}.}

\item{measure}{Medidas de associação a calcular. Um ou mais de:
\code{"count"} (frequência conjunta), \code{"pmi"} (pointwise mutual information),
\code{"dice"} (coeficiente Dice). Padrão: \code{c("count", "pmi")}.}

\item{min_count}{Frequência mínima de co-ocorrência para incluir o par.
Padrão: \code{2}.}

\item{...}{Ignorado.}
}
\value{
Tibble com colunas:
\itemize{
\item \code{word1}, \code{word2}: par de termos (ordenado alfabeticamente);
\item \code{cooc}: frequência de co-ocorrência;
\item \code{pmi} (se solicitado): pointwise mutual information;
\item \code{dice} (se solicitado): coeficiente Dice.
}
}
\description{
\code{ac_cooccurrence()} calcula pares de termos que co-ocorrem dentro de
janelas deslizantes ou dentro do mesmo documento, retornando frequências
e medidas de associação (PMI, Dice).
}
\examples{
# Corpus tematico: coocorrencia revela associacoes conceituais
# (que palavras "andam juntas" no discurso).
df <- data.frame(
  id = paste0("d", 1:6),
  texto = c(
    "democracia participacao voto liberdade cidadania popular",
    "cidadania direitos participacao democracia representacao politica",
    "voto direitos liberdade cidadania soberania popular",
    "mercado economia eficiencia privatizacao competicao livre",
    "privatizacao mercado livre eficiencia produtividade lucro",
    "economia crescimento investimento mercado capital juros"
  ),
  stringsAsFactors = FALSE
)

corpus <- ac_corpus(df, text = texto, docid = id) |>
  ac_clean()
tokens <- ac_tokenize(corpus)

# Janela de 3 tokens, sem filtro de frequencia minima
# (em corpora reais use min_count >= 5 para reduzir ruido)
cooc <- ac_cooccurrence(tokens, window = 3L, min_count = 1L)
head(cooc)

# Coocorrencia com PMI (associacao normalizada por acaso)
cooc_pmi <- ac_cooccurrence(tokens, window = 3L, measure = "pmi",
                            min_count = 1L)
head(cooc_pmi)

}
\seealso{
\code{\link[=ac_tokenize]{ac_tokenize()}}, \code{\link[=ac_plot_cooccurrence]{ac_plot_cooccurrence()}}
}
\concept{quantitative}
